{
    "mcpServers": {
        "cl-mcp": {
            "command": "sbcl",
            "args": ["--noinform", "--non-interactive",
                     "--eval", "(asdf:load-system :cl-mcp)",
                     "--eval", "(cl-mcp:run :transport :stdio)"],
            "env": {
                "MCP_PROJECT_ROOT": "/home/ben/common-lisp/org.sttimothyto"
            }
        }
    }
}
